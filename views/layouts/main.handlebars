<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    
    <!-- SEO Meta Tags -->
    <meta name="description" content="{{metaDescription}}">
    <meta name="keywords" content="{{metaKeywords}}">
    
    <!-- Open Graph Meta Tags (para compartir en redes sociales) -->
    <meta property="og:title" content="{{ogTitle}}">
    <meta property="og:description" content="{{ogDescription}}">
    <meta property="og:image" content="{{ogImage}}">
    <meta property="og:url" content="{{ogUrl}}">
    
    <title>{{title}}</title>

    <!-- Enlaces a CSS externos -->
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="stylesheet" href="/css/responsive.css"> <!-- Opcional, para diseños responsive -->
    
    <!-- Favicon -->
    <link rel="icon" href="/images/favicon.ico" type="image/x-icon">
    
    <!-- Scripts adicionales en el <head> -->
    {{{headScripts}}}
</head>
<body>
    <!-- Barra de navegación fija -->
    <header>
        {{> header }} <!-- Incluir el parcial 'header' -->
    </header>

    <!-- Contenido Principal -->
    <main>
        {{{body}}}  
    </main>

    <!-- Pie de página -->
    <footer>
        {{> footer }} <!-- Incluir el parcial 'footer' -->
    </footer>

    <!-- Scripts comunes -->
    <script src="/js/main.js"></script>

    <!-- Scripts adicionales específicos de cada página -->
    {{{footerScripts}}}
</body>
</html>
